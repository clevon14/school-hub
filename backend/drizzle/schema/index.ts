import { schools } from "./schools";
import { students } from "./students";
import { users } from "./users";

// 👇 THIS IS CRITICAL
export const schema = {
  schools,
  students,
  users,
};

export { schools, students, users };
